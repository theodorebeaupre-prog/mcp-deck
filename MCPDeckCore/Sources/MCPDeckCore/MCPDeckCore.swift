/// MCPDeckCore is the UI-independent engine of MCP Deck: config discovery,
/// health checks, config mutation, and log tailing. It has no SwiftUI/AppKit
/// dependency so it can be unit-tested with `swift test`.
public enum MCPDeckCoreInfo {
    public static let version = "0.1.0"
}
